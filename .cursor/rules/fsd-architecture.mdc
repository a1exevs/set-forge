---
description: Feature-Sliced Design architecture rules
globs: ["client/**"]
---

# Feature-Sliced Design Architecture Rules

## Project Structure

Uses **Feature-Sliced Design (FSD)**. Layer hierarchy:

```
app → pages → widgets → features → entities → shared
```

Layers import only downward. Per layer (except shared): slices → segments.

**Layers:**
- **app/** - Init, providers, global styles
- **pages/** - Page components (routing)
- **widgets/** - Composite UI blocks
- **features/** - User interactions, business features (may be empty until features are added)
- **entities/** - Business entities, domain models
- **shared/** - Utilities, UI components, API clients

**Segments:** `ui/` (components), `model/` (logic, types, query hooks, stores), `api/` (HTTP functions — internal to entity, used by query hooks)

## Import Rules

**Cross-layer — single-word aliases only:**

```typescript
// ✅ CORRECT
import { Button, createSelectors } from '@shared';
import { useWorkoutListsQuery, type WorkoutList, muscleGroupLabels } from '@entities';

// ❌ WRONG
import { Button } from '@shared/ui';
import { WorkoutList } from '@entities/workout-list';
import { useWorkoutListsQuery } from '@entities/workout-list/model/use-workout-queries';
```

**Intra-layer — absolute paths from `src/`:**

```typescript
// ✅ CORRECT
import HomePageLogicLayer from 'src/pages/home/ui/home-page-logic-layer';
```

## Public API Exports

Every layer, slice, and segment MUST have `index.ts` re-exporting public API. Use explicit named exports (`export { X } from '...'`), NOT `export *`. Export only what consumers need.

```typescript
// Layer root: src/pages/index.ts
export { HomePage } from 'src/pages/home';
export { CreateWorkoutPage } from 'src/pages/create-workout';
export { WorkoutModePage } from 'src/pages/workout-mode';

// Slice: src/entities/workout-list/index.ts
export {
  useWorkoutListsQuery,
  useWorkoutQuery,
  workoutQueryKeys,
  type WorkoutList,
} from 'src/entities/workout-list/model';

// Slice: src/entities/exercise/index.ts
export { muscleGroups, muscleGroupLabels, type MuscleGroup } from 'src/entities/exercise/model';
```

## When Creating New Code

1. Pick correct layer
2. Create or use slice
3. Organize by segments (ui, model, api)
4. Add `index.ts` at each level
5. Export only public API via index
6. Use single-word imports from other layers

## Example Structure

```
src/
├── app/
│    ├── index.ts
│    └── model/
│        ├── router
│        ├── routes
│        └── index.ts
├── entities/
│   ├── index.ts
│   ├── exercise/
│   │   ├── index.ts
│   │   └── model/
│   │       ├── index.ts
│   │       ├── types.ts
│   │       └── muscle-group-labels.ts
│   ├── session/
│   │   ├── index.ts
│   │   ├── api/
│   │   ├── model/
│   │   │   ├── use-session-queries.ts
│   │   │   └── session-keys.ts
│   │   └── lib/
│   └── workout-list/
│       ├── index.ts
│       ├── api/
│       │   └── workout-list-api.ts
│       └── model/
│           ├── use-workout-queries.ts
│           ├── workout-query-keys.ts
│           └── types.ts
├── pages/
│   └── home/
│       └── ui/
│           ├── home-page-data-layer.tsx
│           ├── home-page-logic-layer.tsx
│           └── home-page.tsx
└── shared/
    ├── index.ts
    ├── ui/
    │   ├── index.ts
    │   └── button
    │       ├── button.tsx
    │       └── button.module.scss
    └── model/
        ├── index.ts
        ├── theme/
        │   └── store.ts
        └── helpers/
            ├── stores.ts
            └── dates.ts
```
